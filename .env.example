# Database (local Docker Postgres — see docker-compose.yml)
DATABASE_URL="postgresql://ferienhaus:ferienhaus@localhost:5432/ferienhaus"

# JWT signing secret — replace with a long random string in production
JWT_SECRET="change-me-in-production-use-a-long-random-string"

# Initial admin credentials used by prisma/seed.ts
ADMIN_EMAIL="admin@ferienhaus.local"
ADMIN_NAME="Admin"
ADMIN_PASSWORD="changeme123"
